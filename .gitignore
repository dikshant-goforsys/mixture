# Local runtime state — durable session/coordination state, not source.
# (memory store + L4 task ledger live here; both outside the context window)
.mixture/

# Node
node_modules/
*.log

# OS
.DS_Store
.claude/scheduled_tasks.lock
